# CLAUDE.md

このファイルは Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンス。

## Language

- 日本語で回答すること

## Project Overview

Blender アドオン **GUI Slider** のユーザーマニュアル。MkDocs Material 製。
アドオン本体は別リポジトリ（`../gui_slider/`）。

- 公開先: <https://iwashibori.github.io/gui-slider-doc/>
- master への push で GitHub Actions が Pages へ自動デプロイ（`.github/workflows/deploy.yml`）
- 日英2言語。`docs/ja/` と `docs/en/` に同じ構成のページを置く

## Development

```bash
python3 -m mkdocs serve -a 127.0.0.1:8080   # プレビュー（8000 は使用不可）
python3 -m mkdocs build --strict            # リンク切れ検証
```

`mkdocs` コマンドは PATH に無い。`python3 -m mkdocs` で呼ぶこと。

## 執筆方針

### 簡潔に書く

**冗長さは害。** 1項目1行で足りるならそれ以上書かない。特に更新履歴。

- 詳細は既存の解説ページへのリンクに委ね、同じ説明を2箇所に書かない
- 「〜することができます」→「〜できる」。常体で統一
- 前置きや締めの一文（「以上が〜です」等）は書かない

### 利用者が観測できることだけ書く

内部実装の変更は、利用者の画面や操作に現れないなら**書かない**。

- リファクタリング、モジュール分割、内部ヘルパーの追加 → 書かない
- 「〇〇を実装した」ではなく「〇〇できるようになった」と、利用者の利益で書く

### 更新履歴（`docs/*/changelog.md`）

**リリース間の差分ではなく、リリース済み zip 間の差分を書く。** version 番号の変更時点とパッケージ時点がずれるため、`blender_manifest.toml` の履歴を見ると誤る。

`releases/*.zip` のタイムスタンプとコミット時刻を突き合わせ、実際にその zip へ入ったものを基準にすること。実例として v1.1.0 は 3/28 に版数を上げたが zip は 4/4 で、その間の Linked Driver 一式が v1.1.0 に含まれている。

**そのバージョンで初出の機能に対する修正は書かない。** 利用者は修正前の挙動を見ていないため、混乱するだけ。同様に、リリースとリリースの間だけ存在して消えた変更も書かない。

### 構成

「どう作るか（操作の入口）」と「何を設定できるか（機能の属性）」を同じ見出しレベルに混ぜない。前者を先に置く。

新機能を追記するときは、既存の見出しの並びの中でその内容がどの種類に属するかを見てから場所を決める。末尾に足すと階層がちぐはぐになりやすい。

### 日本語見出しのアンカー

MkDocs は日本語見出しに `#_2` `#_3` という**連番 ID** を振る。見出しを1つ挿入すると既存リンクが黙って壊れるため、**リンク先にする見出しには明示 ID を付ける**（`attr_list` が有効）。

```markdown
### 追加方法 { #how-to-add }
```

英語見出しは意味のある slug が生成されるので不要。

### 変更後の確認

`mkdocs build --strict` を通し、生成 HTML のアンカーリンクが実在するか確認してから push すること。`--strict` はページ間リンクは見るが、**アンカー（`#`以降）の生死は検出しない**。

## 注意

- `.gitignore` で `site/` `.claude/` `.DS_Store` を除外済み
- 画像は `docs/images/` に置き、`../../images/` 相対で参照する
